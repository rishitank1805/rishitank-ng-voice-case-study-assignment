apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: db-default-deny-ingress
  namespace: case-study
spec:
  podSelector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - db-primary
          - db-replica
  policyTypes:
    - Ingress

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-web-tier-to-db-5432
  namespace: case-study
spec:
  podSelector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - db-primary
          - db-replica
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: backend
      ports:
        - protocol: TCP
          port: 5432

---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-replica-to-primary-5432
  namespace: case-study
spec:
  podSelector:
    matchLabels:
      app: db-primary
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: db-replica
      ports:
        - protocol: TCP
          port: 5432
